{
  "type": "object",
  "properties": {
    "expSettings": {
      "type": "string",
      "title": "Experimental settings"
    },
    "scanNumbers": {
      "type": "integer",
      "title": "Scan number"
    }
  },
  "required": ["expSettings", "scanNumbers"]
}
